[pytest]
# Only collect the dedicated test suite. Some repos have files like
# generate_hard_test.py that match pytest's *_test.py glob but are scripts, not
# tests, and import heavy deps CI deliberately does not install.
testpaths = tests
